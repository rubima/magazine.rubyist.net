{{*type:      seq*}}                 # シーケンス型
{{*sequence:*}}                      # シーケンスの要素
  - {{*type:      map*}}             #   マッピング型
    {{*mapping:*}}                   #   マッピングの要素
     "name":
        type:      str
        required:  true
     "email":
        type:      str
